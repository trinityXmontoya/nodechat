$(document).ready( function(){
  // Streamgraph.init();
   // $.getJSON('getHistory', function(data){
   //    Streamgraph.init(data.length);
   //  });

  Chat.init();
  // Custom.init();
  // Streamgraph.init();
});
